name: Regenerate base Composer lock

on:
  push:
    branches:
      - fix/composer-update-workflow

permissions:
  contents: write

jobs:
  regenerate-lock:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7
        with:
          ref: fix/composer-update-workflow

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          coverage: none

      - name: Regenerate base Composer lock
        run: composer update mrsleeps/vexim-web-core-settings --with-all-dependencies --minimal-changes --no-interaction --no-progress --prefer-dist --no-scripts

      - name: Commit regenerated lock and remove temporary workflow
        run: |
          rm .github/workflows/regenerate-base-lock.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add composer.lock .github/workflows/regenerate-base-lock.yml
          git commit -m "Update base Composer lock for Core Settings 1.2"
          git push origin HEAD:fix/composer-update-workflow
