authenticated_users {
    priority = 30;
    authenticated = true;
    apply {
        actions {
            reject = 40;     # Much higher threshold for outbound
            add_header = 25; # Add warning at 25
            greylist = null; # No greylisting
        }
    }
}
